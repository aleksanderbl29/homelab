---
name: Yaml Lint
on:
  # push:
  #   branches:
  #     - main
  # pull_request:
  workflow_dispatch:
jobs:
  lintAllTheThings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: yaml-lint
        uses: ibiqlik/action-yamllint@v3